import requests
import json
import sys

# Try to find a way to get a token without password, or use a master token
# Wait, I can generate a token using `auth.create_access_token` since I am on the server.
